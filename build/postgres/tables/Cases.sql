BEGIN TRANSACTION;
CREATE TABLE cases
(
    case_id SERIAL PRIMARY KEY,
    user_id INTEGER,
    time_of_symptoms TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT user_id_constraint FOREIGN KEY (user_id) REFERENCES users(user_id)
    ON UPDATE CASCADE
    ON DELETE CASCADE
);
COMMIT;